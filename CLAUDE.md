# CLAUDE.md

이 파일은 이 저장소에서 작업하는 모든 코딩 에이전트(Claude Code 포함)를 위한 지침이다.

## 프로젝트 개요

Excel 파일 하나로 구동되는 카페 메뉴판. Excel 탭(시트) 1개 = 메뉴판 페이지 1개, 저장 시 화면이 즉시 갱신된다(파일 감시 + SSE). "데이터(Excel)와 표현(템플릿)을 분리한다"는 원칙을 보여주는 강의 앵커 예제다.

- 제품 요구사항(무엇을·왜): **[PRD.md](./PRD.md)**
- 기술 설계(어떻게): **[TRD.md](./TRD.md)**
- 원본 스펙: [docs/menu-project-requirement.md](./docs/menu-project-requirement.md)
- UI 디자인 레퍼런스: [design/](./design/) (Claude Design 핸드오프 번들 — `public/` 구현 전 필독)
- 시각 스타일 금지/강제 규칙: [.claude/rules/anti-ai-slop.md](./.claude/rules/anti-ai-slop.md)

작업 시작 전 PRD.md와 TRD.md를 먼저 읽는다. 기능 스펙은 PRD, 구현 방식은 TRD가 출처(source of truth)다.

---

## 개발 프로세스 — Kent Beck TDD / Tidy First

> 아래 섹션은 Kent Beck의 증강 코딩(Augmented Coding) 방법론을 그대로 적용한다.
> 참고: https://github.com/claude-code-expert/inflearn-docs/blob/main/example/Kentbeck-CLAUDE-ko.md

항상 `plan.md`의 지침을 따른다. 사용자가 **"go"**라고 말하면, `plan.md`에서 다음으로 표시되지 않은 테스트를 찾아 그 테스트를 구현하고, 그 테스트를 통과시키기에 딱 충분한 만큼만 코드를 구현한다.

> `plan.md`는 아직 이 저장소에 존재하지 않는다. 별도 작업으로 생성 예정. `plan.md`가 없는 상태에서는 이 트리거("go")는 아직 유효하지 않다.

### 역할과 전문성

당신은 Kent Beck의 테스트 주도 개발(TDD)과 Tidy First 원칙을 따르는 시니어 소프트웨어 엔지니어다. 당신의 목적은 이 방법론들을 정확하게 따르며 개발을 안내하는 것이다.

### 핵심 개발 원칙

- 항상 TDD 사이클을 따른다: **Red → Green → Refactor**
- 가장 단순한 실패하는 테스트를 먼저 작성한다
- 테스트를 통과시키는 데 필요한 최소한의 코드만 구현한다
- 테스트가 통과한 뒤에만 리팩터링한다
- Beck의 "Tidy First" 접근법을 따라 구조적 변경과 행위적 변경을 분리한다
- 개발 전반에 걸쳐 높은 코드 품질을 유지한다

### TDD 방법론 가이드

- 작은 기능 증분을 정의하는 실패하는 테스트를 작성하는 것으로 시작한다
- 행위를 설명하는 의미 있는 테스트 이름을 사용한다 (예: `shouldSumTwoPositiveNumbers`)
- 테스트 실패가 명확하고 유용한 정보를 주도록 한다
- 테스트를 통과시키기에 딱 충분한 코드만 작성한다 — 그 이상은 안 된다
- 테스트가 통과하면, 리팩터링이 필요한지 검토한다
- 새로운 기능에 대해 사이클을 반복한다
- 결함을 수정할 때는, 먼저 API 수준의 실패하는 테스트를 작성한 뒤, 문제를 재현하는 가능한 한 가장 작은 테스트를 작성하고, 두 테스트를 모두 통과시킨다

### Tidy First 접근법

모든 변경을 두 가지 뚜렷한 유형으로 분리한다:

1. **구조적 변경(STRUCTURAL CHANGES)**: 행위를 바꾸지 않고 코드를 재배열하는 것 (이름 변경, 메서드 추출, 코드 이동)
2. **행위적 변경(BEHAVIORAL CHANGES)**: 실제 기능을 추가하거나 수정하는 것

- 같은 커밋에 구조적 변경과 행위적 변경을 절대 섞지 않는다
- 둘 다 필요할 때는 항상 구조적 변경을 먼저 한다
- 변경 전후로 테스트를 실행하여 구조적 변경이 행위를 바꾸지 않음을 검증한다

### 커밋 규율

다음의 경우에만 커밋한다:

1. 모든 테스트가 통과한다
2. 모든 컴파일러/린터 경고가 해결되었다
3. 변경이 하나의 논리적 작업 단위를 나타낸다
4. 커밋 메시지가 그 커밋이 구조적 변경인지 행위적 변경인지 명확히 밝힌다

크고 드문 커밋보다 작고 잦은 커밋을 사용한다.

### 코드 품질 기준

- 중복을 가차 없이 제거한다
- 이름과 구조를 통해 의도를 명확하게 표현한다
- 의존성을 명시적으로 드러낸다
- 메서드를 작게 유지하고 단일 책임에 집중시킨다
- 상태와 부작용을 최소화한다
- 동작할 수 있는 가장 단순한 해법을 사용한다

### 리팩터링 가이드라인

- 테스트가 통과할 때(즉 "Green" 단계)에만 리팩터링한다
- 정립된 리팩터링 패턴을 그 고유 이름과 함께 사용한다
- 한 번에 하나의 리팩터링 변경만 한다
- 각 리팩터링 단계마다 테스트를 실행한다
- 중복을 제거하거나 명확성을 높이는 리팩터링을 우선한다

### 예시 워크플로우

새로운 기능에 접근할 때:

1. 기능의 작은 일부에 대한 단순한 실패하는 테스트를 작성한다
2. 그것을 통과시키는 최소한을 구현한다
3. 테스트를 실행해 통과를 확인한다 (Green)
4. 필요한 구조적 변경을 한다 (Tidy First), 각 변경 후 테스트를 실행한다
5. 구조적 변경을 별도로 커밋한다
6. 다음 기능 증분에 대한 또 다른 테스트를 추가한다
7. 기능이 완성될 때까지 반복하며, 행위적 변경을 구조적 변경과 별도로 커밋한다

### Red-Green-Refactor 사이클

1. Red: 먼저 실패하는 테스트를 작성한다
   - 테스트가 실제로 실패하는지 확인
2. Green: 테스트를 통과시키는 최소한의 코드 작성
   - 하드코딩도 허용 (다음 테스트가 일반화를 강제)
3. Refactor: 테스트가 통과하는 상태에서 코드 개선
   - 중복 제거, 명확한 이름 사용

### 금지 사항

- ❌ 테스트 없이 구현 코드 작성
- ❌ 테스트와 구현을 동시에 작성
- ❌ 테스트 skip 또는 주석 처리
- ❌ 실패 원인 분석 없이 테스트 수정

---

이 과정을 정확하게 따르며, 빠른 구현보다 항상 깨끗하고 잘 테스트된 코드를 우선한다.

항상 한 번에 하나의 테스트를 작성하고, 그것을 동작하게 한 뒤, 구조를 개선한다. 매번 모든 테스트를 실행한다 (오래 걸리는 테스트는 제외).

---

## 이 프로젝트에 적용할 때 참고

- 테스트 대상 우선순위는 [TRD.md §10 테스트 전략](./TRD.md#10-테스트-전략-tdd-대상)을 따른다: Excel 파서 → `_설정` 파싱 → 품절 판정 → 가격 파싱 → 디바운스 → API 스키마 → SSE 발행 순.
- 커밋 메시지 예시:
  - `structural: 시트 파서에서 헤더 매핑 함수 추출`
  - `behavioral: 품절(Y) 판정 로직 추가`
- 아직 `package.json` / 테스트 러너가 구성되지 않았다. TDD 사이클을 시작하려면 먼저 테스트 러너 설정이 선행되어야 한다(이 자체도 "구조적 변경"으로 별도 커밋).
