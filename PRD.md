# PRD — 카페 메뉴판 (Excel 구동)

> Product Requirements Document. "무엇을, 왜" 만든다. 기술 구현 방법(어떻게)은 [TRD.md](./TRD.md) 참고.
> 원본 요구사항: [docs/menu-project-requirement.md](./docs/menu-project-requirement.md)

## 1. 배경 · 목적

점주가 메뉴/가격을 바꿀 때마다 개발자를 거치지 않고, **Excel 파일 하나**로 메뉴판 화면을 직접 운영할 수 있게 한다.

이 프로젝트는 동시에 강의용 앵커 예제이기도 하다. 핵심 메시지는 하나:

> **데이터(Excel)와 표현(템플릿)을 분리한다.**

같은 데이터가 CSS 테마만 바뀌어도 완전히 다른 화면으로 보여야 하고, 점주는 코드를 건드리지 않고 Excel만 편집해야 한다.

## 2. 대상 사용자

| 사용자 | 니즈 |
|---|---|
| 카페 점주 (비개발자) | Excel 편집만으로 메뉴/가격/품절/테마를 즉시 바꾸고 싶다. 코드·배포 지식 불필요해야 한다. |
| 매장 손님 (화면 뷰어) | 메뉴판이 항상 최신 상태여야 하고, 읽기 쉬워야 한다. |
| 수강생 (강의 맥락) | "데이터/표현 분리" 원칙을 눈으로 확인하고 싶다. |

## 3. 핵심 사용자 시나리오

1. 점주가 `data/menu.xlsx`를 Excel에서 연다.
2. 가격을 수정하거나, 품절 컬럼에 `Y`를 입력하거나, `_설정` 시트에서 테마를 바꾼다.
3. `Ctrl+S`로 저장한다.
4. 브라우저에 띄워둔 메뉴판이 새로고침 없이 자동으로 갱신된다.

부가 시나리오:
- 점주가 새 탭(시트)을 추가하면 → 메뉴판에 새 페이지가 자동으로 생긴다.
- 매장에 태블릿/모니터를 세워두고(디지털 사이니지) `자동전환초`를 설정하면 → 페이지가 자동으로 순환한다.

## 4. 기능 요구사항

### 4.1 메뉴 시트

- 시트(탭) 이름 = 메뉴판 페이지 이름. `_`로 시작하지 않는 모든 시트는 페이지로 노출된다.
- 첫 행은 헤더. 컬럼: `메뉴명 | 가격 | 설명 | 품절 | 카테고리`.
- **가격**: 숫자만 입력(₩·콤마 금지). 화면에는 `원` 단위를 붙여 표시.
- **품절**: `Y` 또는 빈칸. `Y`면 취소선 + 흐림 처리 + "품절" 라벨 표시.
- **카테고리**: 같은 페이지 안에서 소제목 그룹으로 사용 가능.

### 4.2 `_설정` 시트

`_` 접두사 시트는 페이지로 노출되지 않고 전역 설정으로만 쓰인다.

| 항목 | 값 | 동작 |
|---|---|---|
| 매장명 | 텍스트 | 메뉴판 상단에 표시 |
| 테마 | 테마 키(예: `cafe-dark`, `deep-green` 등 8종) | 코드 수정 없이 화면 디자인 전환 |
| 디바이스 | `signage`\|`tablet-land`\|`tablet-port`\|`mobile` | 목표 해상도/비율 결정(CLAUDE.md #해상도) |
| 영문태그 | 텍스트 | 메뉴판 상단 영문 태그라인 표시 |
| 자동전환초 | 숫자 | 0 = 수동 전환, 양수 = 해당 초마다 페이지 자동 전환 |

### 4.3 실시간 갱신

- Excel 저장 시점(디스크에 실제로 기록되는 시점)에만 반영된다. 로컬 `.xlsx`는 저장 전까지는 반영되지 않음을 사용자에게 인지시켜야 한다. (OneDrive 자동저장 환경은 예외적으로 즉시 반영될 수 있음)
- 화면은 새로고침 없이 자동 갱신되어야 한다.
- 관리자 화면(4.5)에서의 저장도 동일한 파일 감시 파이프라인을 사용하므로 동일하게 즉시 반영된다.

### 4.4 다중 페이지 · 자동 전환

- 시트 개수만큼 페이지가 생긴다.
- `자동전환초` > 0이면 해당 주기로 페이지를 순환 표시한다(키오스크/사이니지 용도).

### 4.5 관리자 설정 화면 (`/admin`)

점주가 Excel 앱을 열지 않고도 `_설정` 시트 값을 웹 화면에서 바꿀 수 있다.

- `/admin`에서 아이디/비밀번호(`admin`/`1234`, 고정값)로 로그인한다. 매장당 운영자 1명을 가정한 최소 보호 장치이며, 다중 사용자·권한 체계는 아니다.
- 로그인 후 `design/project/메뉴판 템플릿 시스템.dc.html` 목업과 동일한 방식(버튼 선택)으로 테마·디바이스를 고르고, 매장명·영문 태그·자동전환초를 입력 필드로 수정한다.
- 저장하면 `data/menu.xlsx`의 `_설정` 시트에 즉시 반영되고, 기존 파일 감시(chokidar) → SSE 파이프라인을 그대로 타고 `/` 화면에 새로고침 없이 반영된다.
- `/`(정면 화면)에는 조정 UI가 노출되지 않는다 — 저장된 설정값에 해당하는 전체화면 메뉴판만 보여준다.
- 메뉴 항목(가격/품절/설명 등)은 관리자 화면의 범위가 아니다. 여전히 Excel 앱에서 직접 편집한다(5절 참고).

## 5. 비범위 (초기 버전에서 제외)

- Excel 파일에서 메뉴 항목(가격/품절/설명 등)을 브라우저에서 직접 편집하는 기능 (편집은 Excel 앱에서만). 단, `_설정` 값은 4.5의 관리자 화면에서 조정 가능.
- 다중 매장/다중 파일 지원.
- 다중 사용자·세분화된 권한 관리(관리자 로그인은 고정 계정 1개로 최소 보호만 제공, 4.5 참고).
- 원격(사내 파일서버) 공유 방식의 폴링(ETag) — "응용 아이디어"로만 남겨둔다.

## 6. 완료 정의 (Definition of Done)

- [ ] Excel의 메뉴 시트를 저장하면 3초 이내에 브라우저 화면이 갱신된다.
- [ ] `_설정` 시트는 페이지 목록에 나타나지 않는다.
- [ ] 품절(`Y`) 항목이 취소선 + 흐림 + 라벨로 구분된다.
- [ ] 테마를 `cafe-dark` ↔ `bistro-light`로 바꾸면 코드 수정 없이 화면이 바뀐다.
- [ ] `자동전환초`가 양수일 때 설정한 주기로 페이지가 자동 전환된다.
- [ ] 시트를 추가/삭제하면 페이지 수가 그에 맞게 늘거나 준다.

## 7. 확장 백로그 (강의 응용 아이디어)

- 템플릿 CSS 추가로 테마 종류 확장 (데이터는 그대로, CSS만 교체).
- HTTP 공유 방식: 사내 파일서버 + 브라우저 폴링(ETag).
- 키오스크 모드: 브라우저 전체화면 + `자동전환초` 순환.
